import GameResult from './GameResult'

interface TenhouLog {
  roomNumber: string
  roomType: string
  startTime: string
  gameResultsTest: string
  gameResults: GameResult[]
}
export default TenhouLog
